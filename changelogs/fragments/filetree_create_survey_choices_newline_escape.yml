bugfixes:
  - filetree_create - fix survey choices newline escaping so multiplechoice/multiselect options round-trip as separate choices instead of a single literal ``yes\\nno`` value
  - filetree_create - export boolean survey defaults/choices (``true``/``false``) as ``!unsafe`` strings so the API does not reject them as non-strings
